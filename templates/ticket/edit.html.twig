{% extends 'base.html.twig' %}

{% block title %}Modifier un tickets{% endblock %}

{% block body %}
<div class="container">

    <h1 class="text-center">Modifier un ticket</h1>
    <a href="{{ path('ticket_index') }}" class="btn btn-outline-dark mt-3 mb-3">Retour à la liste</a>
    
    {{ include('ticket/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    
    
    {{ include('ticket/_delete_form.html.twig') }}
</div>
{% endblock %}
